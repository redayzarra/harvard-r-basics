# In R, you can assign variables by using the assign symbol
a <- 1
b <- 1
c <- -1

print(a) # Allows us to print the variable

ls() # Shows us all the variables saved in the work space (environment)

(-b + sqrt(b^2 - 4*a*c)) / (2*a)

(-b - sqrt(b^2 - 4*a*c)) / (2*a)